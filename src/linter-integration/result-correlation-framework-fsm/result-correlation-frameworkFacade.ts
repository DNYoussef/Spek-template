// result-correlation-framework Facade - FSM Range 800-899
// Generated by Range-Based Eliminator

// TODO(Phase 4): Implement core module - import { ResultCorrelationFrameworkCore } from './ResultCorrelationFrameworkCore';
import { ResultCorrelationFrameworkStateMachine } from './ResultCorrelationFrameworkStateMachine';

export class ResultCorrelationFrameworkFacade {
    private core: ResultCorrelationFrameworkCore;
    private fsm: ResultCorrelationFrameworkStateMachine;

    constructor() {
        this.core = new ResultCorrelationFrameworkCore();
        this.fsm = new ResultCorrelationFrameworkStateMachine();
    }

    // Unified interface preserving original API
    async processRequest(request: any): Promise<any> {
        this.fsm.transition('START', request);
        const result = await this.core.process(request);
        this.fsm.transition('COMPLETE', result);
        return result;
    }
}

// Export original API compatibility
export default ResultCorrelationFrameworkFacade;
