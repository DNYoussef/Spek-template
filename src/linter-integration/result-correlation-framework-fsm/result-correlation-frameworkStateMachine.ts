// result-correlation-framework State Machine - FSM Range 800-899
// Generated by Range-Based Eliminator

export enum result-correlation-frameworkEvents {
    START = 'START',
    PROCESS = 'PROCESS',
    VALIDATE = 'VALIDATE',
    COMPLETE = 'COMPLETE',
    ERROR = 'ERROR'
}

export interface result-correlation-frameworkState {
    name: string;
    data?: any;
}

export class result-correlation-frameworkStateMachine {
    private currentState: result-correlation-frameworkState;

    constructor() {
        this.currentState = { name: 'initial' };
    }

    transition(event: result-correlation-frameworkEvents, data?: any): void {
        switch (this.currentState.name) {
            case 'initial':
                if (event === result-correlation-frameworkEvents.START) {
                    this.currentState = { name: 'processing', data };
                }
                break;
            case 'processing':
                if (event === result-correlation-frameworkEvents.COMPLETE) {
                    this.currentState = { name: 'completed', data };
                }
                break;
        }
    }

    getState(): result-correlation-frameworkState {
        return this.currentState;
    }
}
