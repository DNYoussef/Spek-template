// result-correlation-framework State Machine - FSM Range 800-899
// Generated by Range-Based Eliminator

export enum ResultCorrelationFrameworkEvents {
    START = 'START',
    PROCESS = 'PROCESS',
    VALIDATE = 'VALIDATE',
    COMPLETE = 'COMPLETE',
    ERROR = 'ERROR'
}

export interface ResultCorrelationFrameworkState {
    name: string;
    data?: any;
}

export class ResultCorrelationFrameworkStateMachine {
    private currentState: ResultCorrelationFrameworkState;

    constructor() {
        this.currentState = { name: 'initial' };
    }

    transition(event: ResultCorrelationFrameworkEvents, data?: any): void {
        switch (this.currentState.name) {
            case 'initial':
                if (event === ResultCorrelationFrameworkEvents.START) {
                    this.currentState = { name: 'processing', data };
                }
                break;
            case 'processing':
                if (event === ResultCorrelationFrameworkEvents.COMPLETE) {
                    this.currentState = { name: 'completed', data };
                }
                break;
        }
    }

    getState(): ResultCorrelationFrameworkState {
        return this.currentState;
    }
}
