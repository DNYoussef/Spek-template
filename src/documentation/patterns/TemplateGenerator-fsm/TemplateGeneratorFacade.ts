// TemplateGenerator Facade - FSM Range 600-699
// Generated by Range-Based Eliminator

// TODO(Phase 4): Implement core module - import { TemplateGeneratorCore } from './TemplateGeneratorCore';
import { TemplateGeneratorStateMachine } from './TemplateGeneratorStateMachine';

export class TemplateGeneratorFacade {
    private core: TemplateGeneratorCore;
    private fsm: TemplateGeneratorStateMachine;

    constructor() {
        this.core = new TemplateGeneratorCore();
        this.fsm = new TemplateGeneratorStateMachine();
    }

    // Unified interface preserving original API
    async processRequest(request: any): Promise<any> {
        this.fsm.transition('START', request);
        const result = await this.core.process(request);
        this.fsm.transition('COMPLETE', result);
        return result;
    }
}

// Export original API compatibility

// Backward compatibility

// Backward compatibility
export default TemplateGeneratorFacade;
