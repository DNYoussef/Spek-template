// types State Machine - FSM Range 600-699
// Generated by Range-Based Eliminator

export enum typesEvents {
    START = 'START',
    PROCESS = 'PROCESS',
    VALIDATE = 'VALIDATE',
    COMPLETE = 'COMPLETE',
    ERROR = 'ERROR'
}

export interface typesState {
    name: string;
    data?: any;
}

export class typesStateMachine {
    private currentState: typesState;

    constructor() {
        this.currentState = { name: 'initial' };
    }

    transition(event: typesEvents, data?: any): void {
        switch (this.currentState.name) {
            case 'initial':
                if (event === typesEvents.START) {
                    this.currentState = { name: 'processing', data };
                }
                break;
            case 'processing':
                if (event === typesEvents.COMPLETE) {
                    this.currentState = { name: 'completed', data };
                }
                break;
        }
    }

    getState(): typesState {
        return this.currentState;
    }
}
