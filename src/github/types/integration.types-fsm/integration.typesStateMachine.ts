// integration.types State Machine - FSM Range 500-599
// Generated by Range-Based Eliminator

export enum integration.typesEvents {
    START = 'START',
    PROCESS = 'PROCESS',
    VALIDATE = 'VALIDATE',
    COMPLETE = 'COMPLETE',
    ERROR = 'ERROR'
}

export interface integration.typesState {
    name: string;
    data?: any;
}

export class integration.typesStateMachine {
    private currentState: integration.typesState;

    constructor() {
        this.currentState = { name: 'initial' };
    }

    transition(event: integration.typesEvents, data?: any): void {
        switch (this.currentState.name) {
            case 'initial':
                if (event === integration.typesEvents.START) {
                    this.currentState = { name: 'processing', data };
                }
                break;
            case 'processing':
                if (event === integration.typesEvents.COMPLETE) {
                    this.currentState = { name: 'completed', data };
                }
                break;
        }
    }

    getState(): integration.typesState {
        return this.currentState;
    }
}
