// swarm-types Facade - FSM Range 600-699
// Generated by Range-Based Eliminator

import { swarm-typesCore } from './swarm-typesCore';
import { swarm-typesStateMachine } from './swarm-typesStateMachine';

export class swarm-typesFacade {
    private core: swarm-typesCore;
    private fsm: swarm-typesStateMachine;

    constructor() {
        this.core = new swarm-typesCore();
        this.fsm = new swarm-typesStateMachine();
    }

    // Unified interface preserving original API
    async processRequest(request: any): Promise<any> {
        this.fsm.transition('START', request);
        const result = await this.core.process(request);
        this.fsm.transition('COMPLETE', result);
        return result;
    }
}

// Export original API compatibility
export default swarm-typesFacade;
